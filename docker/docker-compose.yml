services:
  helioai:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "7890:7890"
    volumes:
      - ../data:/app/data
    env_file:
      - ../.env
    restart: unless-stopped
